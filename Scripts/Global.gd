extends Node

var dollars := 0

var cups_of_lemonade := 0

var lemonade_production_speed := 1.00
var customer_spawn_rate := 3.00
var lemon_spawn_rate = 5.00
var customer_move_speed = 100

var lemon_speed = 200
var lemonade_cost = 1

# Called when the node enters the scene tree for the first time.
func _ready():
	pass


# Called every frame. 'delta' is the elapsed time since the previous frame.
func _process(delta):
	pass
